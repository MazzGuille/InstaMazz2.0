@model InstaMazz2._0.Models.UsuarioModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>InstaMazz Login</title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" integrity="sha384-gfdkjb5BdAXd+lj+gudLWI+BXq4IuLW5IT+brZEZsLFm++aCMlF1V92rMkPaX4PP" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.3/font/bootstrap-icons.css">
    <link href="~/Content/CSS/Login.css" rel="stylesheet" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.10.4/gsap.min.js"></script>
</head>
<body>

    <section class="vh-100">
        <div class="container py-1 h-100">
            <div class="row d-flex align-items-center justify-content-center h-50">  
                <div class="col-md-8 col-lg-7 col-xl-6">
                    <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/draw2.svg"
                         class="img-fluid" alt="Phone image">
                </div>
                <div class="col-md-7 col-lg-5 col-xl-6"> 
                    
                    <form action="@Url.Action("Registrar", "Acceso")" method="post" enctype="multipart/form-data">

                        <!-- Email input -->
                        @*<input type="email" name="email" class="form-control input_user" value="" placeholder="E-Mail">*@

                        <div class="form-outline mb-1">
                            <input type="email" name="email" id="form1Example13" class="form-control" />
                            <label class="form-label" for="form1Example13"><i class="bi bi-envelope-fill text-primary"></i> E-Mail</label>
                        </div>

                        <!-- Nombre input -->
                        <div class="form-outline mb-1">
                            @*<input type="Text" name="Nombre" class="form-control input_pass" placeholder="Nombre">*@
                            <input type="text" name="Nombre" id="form1Example23" class="form-control" />
                            <label class="form-label" for="form1Example23"><i class="bi bi-person-fill text-primary"></i> Nombre completo</label>
                        </div>

                        <!-- UserName input -->
                        <div class="form-outline mb-1">
                            @*<input type="Text" name="UserName" class="form-control input_pass" placeholder="Nombre de usuario">*@
                            <input type="text" name="UserName" id="form1Example23" class="form-control" />
                            <label class="form-label" for="form1Example23"><i class="bi bi-person-fill text-primary"></i> Nombre de usuario</label>
                        </div>

                        <!-- Imagen de perfil input -->
                        <div class="form-outline mb-1">
                            @*input name="ImagenPerfil" type="file" class="form-control input_pass">*@
                            <input type="file" name="ImagenPerfil" id="form1Example23" class="form-control" />
                            <label class="form-label" for="form1Example23"><i class="bi bi-image-fill text-primary"></i> Imagén de perfil</label>
                        </div>

                        <!-- Biografia input -->
                        <div class="form-outline mb-1">
                            <textarea type="text" name="BioUsuario" id="form1Example23" class="form-control"></textarea>
                        </div>
                        <p><i class="bi bi-map-fill text-primary"></i> Biografia</p>

                        <!-- Password input -->
                        <div class="form-outline mb-1">
                            @*<input type="password" name="Contraseña" class="form-control input_pass" value="" placeholder="Contraseña">*@
                            <input type="password" name="Contraseña" id="form1Example23" class="form-control" />
                            <label class="form-label" for="form1Example23"><i class="bi bi-key-fill text-primary"></i> Contraseña</label>
                        </div>

                        <!-- Confirmar contraseña input -->
                        <div class="form-outline mb-1">
                            @*<input type="password" name="Contraseña" class="form-control input_pass" value="" placeholder="Contraseña">*@
                            <input type="password" name="ConfirmarClave" id="form1Example23" class="form-control" />
                            <label class="form-label" for="form1Example23"><i class="bi bi-key-fill text-primary"></i> Contraseña</label>
                        </div>

                        @if (ViewData["email"] != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewData["email"]
                            </div>
                        }


                        @if (ViewData["Nombre"] != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewData["Nombre"]
                            </div>
                        }

                        @if (ViewData["NombreUsuario"] != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewData["NombreUsuario"]
                            </div>
                        }

                        @if (ViewData["Mensaje"] != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewData["Mensaje"]
                            </div>
                        }

                        @if (ViewBag.NombreNull != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewBag.NombreNull
                            </div>
                        }

                        @if (ViewBag.UserNameNull != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewBag.UserNameNull
                            </div>
                        }

                        @if (ViewBag.MailNull != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewBag.MailNull
                            </div>
                        }

                        @if (ViewData["Mensaje2"] != null)
                        {
                            <div class="alert alert-danger" role="alert">
                                @ViewData["Mensaje2"]
                            </div>
                        }

                        <!-- Submit button -->
                        <button type="submit" class="btn text-primary border border-primary btn-block">Crear cuenta</button>
                        <div class="mt-4">
                            <div class="d-flex justify-content-center links">
                                ¿Ya tienes una cuenta? <a href="@Url.Action("Login", "Acceso")" class="ml-2">¡Ingresa aquí!</a>
                            </div>
                        </div>

                    </form>
                    </div>
                </div>
        </div>
    </section>

</body>
</html>





<!--<div class="user_card" style="width:30vw; margin: auto; margin-top: 10vh;">
    <div class="d-flex justify-content-center">
        <div class="brand_logo_container">
            <img src="~/Content/CSS/InstaMazz.jpg" class="brand_logo" alt="Logo">
        </div>
    </div>
    <div class="d-flex justify-content-center form_container">
        <form action="@Url.Action("Registrar", "Acceso")" method="post" enctype="multipart/form-data">
            <div class="input-group mb-3">
                <div class="input-group-append">
                    <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                </div>
                <input type="email" name="email" class="form-control input_user" placeholder="E-Mail">
            </div>

            <div class="input-group mb-2">
                <div class="input-group-append">
                    <span class="input-group-text"><i class="bi bi-person"></i></span>
                </div>
                <input type="Text" name="Nombre" class="form-control input_pass" placeholder="Nombre">

            </div>


            <div class="input-group mb-2">
                <div class="input-group-append">
                    <span class="input-group-text"><i class="bi bi-person-badge"></i></span>
                </div>
                <input type="Text" name="UserName" class="form-control input_pass" placeholder="Nombre de usuario">
            </div>
            <div class="input-group mb-2">
                <div class="input-group-append">
                    <span class="input-group-text"><i class="bi bi-image-fill"></i></span>
                </div>
                <input type="file" name="ImagenPerfil" class="form-control input_pass">-->
                @*<input name="ImagenPerfil" type="file" class="form-control input_pass">*@
                @*<p>Opcional</p>*@
            <!--</div>

            <div class="input-group mb-2">
                <div class="input-group-append">
                    <span class="input-group-text"><i class="bi bi-map"></i></span>
                </div>
                <textarea type="Text" name="BioUsuario" class="form-control input_pass" placeholder="Biografia"></textarea>
            </div>

            <div class="input-group mb-2">
                <div class="input-group-append">
                    <span class="input-group-text"><i class="fas fa-key"></i></span>
                </div>
                <input type="password" name="Contraseña" class="form-control input_pass" placeholder="Contraseña">
            </div>
            <div class="input-group mb-2">
                <div class="input-group-append">
                    <span class="input-group-text"><i class="fas fa-key"></i></span>
                </div>
                <input type="password" name="ConfirmarClave" class="form-control input_pass" placeholder="Confirmar Contraseña">
            </div>

            @if (ViewData["email"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["email"]
                </div>
            }


            @if (ViewData["Nombre"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["Nombre"]
                </div>
            }

            @if (ViewData["NombreUsuario"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["NombreUsuario"]
                </div>
            }

            @if (ViewData["Mensaje"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["Mensaje"]
                </div>
            }

            @if (ViewBag.NombreNull != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewBag.NombreNull
                </div>
            }

            @if (ViewBag.UserNameNull != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewBag.UserNameNull
                </div>
            }

            @if (ViewBag.MailNull != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewBag.MailNull
                </div>
            }

            @if (ViewData["Mensaje2"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["Mensaje2"]
                </div>
            }

            <div class="d-flex justify-content-center mt-3 login_container">
                <button type="submit" class="btn login_btn">Crear cuenta</button>
            </div>
        </form>
    </div>

    <div class="mt-4">
        <div class="d-flex justify-content-center links">
            ¿Ya tienes una cuenta? <a href="@Url.Action("Login", "Acceso")" class="ml-2">¡Ingresa aquí!</a>
        </div>
    </div>
</div>


<script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
<script>
</script>-->